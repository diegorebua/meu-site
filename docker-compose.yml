version: '3.8'

services:
  backend:
    build: ./backend
    expose:
      - "3001"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
